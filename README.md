# Inventory API

## Setup Instructions

### 1. Clone Repository

```bash
git clone https://github.com/Garima1005/inventory-api
cd inventory-api

npm install

cp .env.example .env

npm run dev

# Server Runs On
http://localhost:5000

# Notes
# Make sure Node.js is installed
# SQLite database will be created automatically
# Add valid ImageKit credentials in .env
# JWT secret is required for authentication

